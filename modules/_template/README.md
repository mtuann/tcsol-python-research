# Module Template

Copy this folder when starting a new V2 module.

Replace every `MODULE_XX` placeholder and fill the module with real data, notebook output, figure critique, assignment, and rubric.

Required learner flow:

```text
Research problem -> Dataset -> Data operation -> Figure decision -> Paper sentence
```

Do not publish private or identifiable data in this folder.
