# Assignment: Cleaned Dataset and Cleaning Note

## Goal

- Python skill: use pandas to clean a small messy dataset.
- Research skill: make data preparation decisions visible.
- Paper section supported: Methods / Data preparation.
- Writing output: cleaning decision note, table caption, interpretation paragraph, and a before/after cleaning table.

## Data

- File: `data/raw/week04_messy_tcsol_scores.csv`
- Unit of observation: one learner pre/post score record.
- Key variables: `class_group`, `activity_focus`, `pre_score`, `post_score`, `attendance_hours`, `completed`, `self_confidence`.

## Tasks

1. Load and inspect the raw dataset.
2. Create a cleaning log table.
3. Normalize class labels.
4. Normalize activity focus labels.
5. Normalize completed status.
6. Convert numeric columns safely.
7. Create `gain_score`.
8. Export cleaned CSV.
9. Export cleaning log CSV.
10. Export one before/after cleaning summary.
11. Export one usable-rows-by-activity table.
12. Write a 100-150 word cleaning decision note.
13. Write one caption and one 80-120 word interpretation for the before/after table.

## Submission

Submit:

- completed `.ipynb` notebook;
- cleaned dataset CSV;
- cleaning log CSV;
- before/after cleaning summary table;
- usable rows by activity table;
- cleaning decision note;
- caption and interpretation paragraph for the before/after table;
- one limitation;
- source note with access date.

## Expected Checks

Use these values to check that your workflow is running correctly:

| Check | Expected result |
|---|---:|
| Raw rows | 36 |
| Raw `activity_focus` variants | 18 |
| Cleaned `activity_focus` labels | 4 |
| Raw `class_group` variants | 12 |
| Cleaned `class_group` labels | 4 |
| Raw literal `completed == "yes"` usable rows | 19 |
| Cleaned usable completed pre/post rows | 25 |

## Writing Frame

Use this frame:

```text
Before analysis, the raw dataset was preserved and a cleaned copy was created in pandas. Labels in [columns] were standardized using a predefined mapping. Numeric columns were converted with [method], and invalid entries were treated as missing rather than replaced with zero. After cleaning, [N] records had complete pre/post scores for the descriptive summary. The main limitation is [limitation].
```

Mention the exported artifacts by name when possible: `week04_cleaning_log.csv`, `week04_cleaning_summary.csv`, and `week04_usable_by_activity.csv`.

## Limitation Menu

Choose one limitation and adapt it:

- the data are synthetic teaching data, not real student records;
- missing scores are not recoverable from cleaning alone;
- label mappings are researcher decisions and should be documented;
- completed-status normalization affects which rows enter the pre/post summary.

## Source Note Template

```text
Dataset source: Synthetic Week 04 teaching dataset created for this course; not real student data.
Raw file: data/raw/week04_messy_tcsol_scores.csv
Access date: 2026-06-03
Version/check: raw file preserved; cleaned outputs generated by notebook code.
```

## Transfer Note

Add one sentence for your research track. Example:

> If this were an education policy dataset, I would standardize ministry/source names and date fields before counting policy changes.

## Checklist Before Submitting

- [ ] Raw CSV is not manually edited.
- [ ] Cleaned CSV is exported to `data/processed`.
- [ ] Cleaning log has step, column, problem, decision, reason, and evidence.
- [ ] Missing values are not silently replaced with zero.
- [ ] Label mappings are visible in the notebook.
- [ ] Before/after table has a caption, interpretation, N/source note, and matching expected checks.
- [ ] Cleaning decision note avoids saying the data are perfect.
